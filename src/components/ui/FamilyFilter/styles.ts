import {SafeAreaView} from 'react-native';
import styled from 'styled-components/native';

export const FamilyFilterContainer = styled(SafeAreaView)`
  padding: 15px;
  margin-bottom: 5px;
  margin-top: 5px;
`;
